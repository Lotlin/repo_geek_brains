# Создать (не программно) текстовый файл со следующим содержимым:
# One — 1
# Two — 2
# Three — 3
# Four — 4
# Необходимо написать программу, открывающую файл на чтение и считывающую построчно данные.
# При этом английские числительные должны заменяться на русские.
# Новый блок строк должен записываться в новый текстовый файл.

user_dict = dict(One='Один', Two='Два', Three='Три', Four='Четыре')       # make a dictionary for changing numerals

with open('4.txt', 'r', encoding='utf8') as file:
    for line in file:
        new_list = line.split()
        new_list[0] = user_dict.get(line.split()[0])                      # change numerals for Russian
        result = " ".join([str(_) for _ in new_list])                     # make a new text
        with open('4_1.txt', 'a', encoding='utf8') as new_file:           # write a new file
            new_file.write(result + '\n')
