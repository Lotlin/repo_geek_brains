# Создать текстовый файл (не программно), построчно записать фамилии сотрудников и величину их окладов.
# Определить, кто из сотрудников имеет оклад менее 20 тыс., вывести фамилии этих сотрудников.
# Выполнить подсчет средней величины дохода сотрудников.

second_list = []
with open('3.txt', 'r', encoding='utf8') as file:
    for line in file:
        lab_sal = line.split()                                     # get info about one person (surname and salary)
        second_list.append(float(lab_sal[1]))                      # mart a list with all employees and salaries
        if float(lab_sal[1]) < 20000:                              # get underpaid employee
            print(f'Сотрудник {lab_sal[0]} незаслуженно получает меньше 20000 рублей.')

    total_salary = 0
    for salary in second_list:
        total_salary += salary                                     # for getting average salary count total salary
    average_salary = round(total_salary / len(second_list), 2)     # get average salary
    print(f'В то время как средний оклад сотрудников составляет {average_salary} рублей.')
